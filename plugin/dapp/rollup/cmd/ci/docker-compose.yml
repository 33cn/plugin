version: '3'

services:
  main:
    build:
      context: .
    environment:
      ChainConfig: "chain33.test.toml"
#    expose:
#      - "8802"

  para1:
    build:
      context: .
    environment:
      ChainConfig: "chain33.para1.toml"

  para2:
    build:
      context: .
    environment:
      ChainConfig: "chain33.para2.toml"
