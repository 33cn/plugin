version: '3'
# 主链
services:
  main:
    build:
      context: .
    environment:
      ChainConfig: "chain33.evm.test.toml"

    expose:
      - 8545
      - 8801


  para1:
    build:
      context: .
    environment:
      ChainConfig: "chain33.para1.toml"

  para2:
      build:
        context: .
      environment:
        ChainConfig: "chain33.para2.toml"
